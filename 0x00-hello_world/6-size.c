#include <stdio.h>
/** Prints the size of various types
 * main-entry point
 * return success 0 */
int main (void)
{
printf("The size of a char is %i bytes", 1); ("The size of in is %i", 4;)
return (0)
}
