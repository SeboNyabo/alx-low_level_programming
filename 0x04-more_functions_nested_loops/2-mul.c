#include "main.h"
/**
 * int mul(int a, int b)- multiplies two integers
 * @a - is multiplied by b
 * @b - is multiplied by a
 * return the results
 */
int mul(int a, int b)
{
	int results;

	results = a * b;
		return (results);
}
