#include "main.h"
/**
 * int _isdigit(int c) - checks for a digit (0 through 9)
 * Returns 1 if c is a digit
 * Returns 0 otherwise
 */

int _isdigit(int c)
{
	int i;

	if (i >= '0' && i <='9' )
	{
	return (1);
	}
}
