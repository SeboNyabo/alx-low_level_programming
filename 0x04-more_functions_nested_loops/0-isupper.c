#include "main.h"
/**
 * isupper checks if a letter is uppercase
 * returns 1 for uppercase and 0for lower case
 */
int _isupper(int c)

{
	char ch;

	if (ch >= 65 && ch <= 90)
			{
			return (1);
		}
		return (0);
}
