#include "main.h"
/**
 * main - entry point
 * _isupper - checks if a letter is uppercase
 * @c:input
 *
 * return: 1 for uppercase and 0 or lower case
 */
int _isupper(int c)

{
        char ch;

        if ((ch >= 'A') && (ch <= 'Z');)
        {
                return (1);
        }
                return (0);

